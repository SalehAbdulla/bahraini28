# Backend API base URL. Leave empty to use the same-origin default "/api/v1"
# (the Vite dev server proxies /api to the backend; Caddy does the same in
# production). Override only for exotic deployments.
VITE_API_URL=